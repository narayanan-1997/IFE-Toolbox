#include<string>
const int BUFFER_SIZE = 100000;
/// compress_log is a header file which defines the
/// prototype for the function compress_file in the main.

void compress_file(const std::string & file_name);
